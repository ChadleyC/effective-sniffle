import React from 'react';

const LoginPage: React.FC = () => {
    return (
        <div className="login-page">
            <h1>Login</h1>
            {/* Login Form */}
        </div>
    );
};

export default LoginPage;
